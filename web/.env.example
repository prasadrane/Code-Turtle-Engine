# (Optional) Backend API endpoint for live GitHub PR reviews.
# If unset, defaults to http://localhost:5000 in development.
# The web app also contains a built-in offline demo replay mode ("Try Demo") that requires no backend.
NEXT_PUBLIC_API_BASE=
